export const metadata = {
  title: 'Links — Fundació Predator',
  robots: 'noindex, nofollow',
};

export default function LinkLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
